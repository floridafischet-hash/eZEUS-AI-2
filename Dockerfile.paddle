FROM python:3.12-slim
ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1 HOME=/home/ezeus
WORKDIR /app
RUN apt-get update \
    && apt-get install -y --no-install-recommends libgl1 libglib2.0-0 libgomp1 \
    && rm -rf /var/lib/apt/lists/*
COPY . .
RUN pip install --no-cache-dir '.[ocr-paddle]'
RUN addgroup --system ezeus && adduser --system --ingroup ezeus --home /home/ezeus ezeus \
    && mkdir -p /home/ezeus \
    && chown -R ezeus:ezeus /app /home/ezeus
CMD ["sh", "-c", "chown -R ezeus:ezeus /home/ezeus/.paddlex && exec su -s /bin/sh ezeus -c 'celery -A core.queue.celery_app.celery_app worker -Q high,normal,low --loglevel=INFO'"]
